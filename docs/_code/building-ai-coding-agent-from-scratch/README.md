# Building AI Coding Agent from Scratch

Leif is a small coding agent: a loop, a model, tools, memory, plan/act, then explore.

```zsh
uv sync
cp .env.example .env   # set OPENROUTER_API_KEY
uv run python main.py
uv run python main.py --act
```
